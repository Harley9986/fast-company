import React from "react";

const SearchString = () => {
    return (
        <>
            <input type="text" className="" placeholder="Search" />
        </>
    );
};

export default SearchString;
